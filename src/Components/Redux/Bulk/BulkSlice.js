import { createSlice } from '@reduxjs/toolkit';

const BulkSlice = createSlice({
  name: 'auth',
  initialState: {
    
  },
  reducers: {
    
  },
});

export const {  } = BulkSlice.actions;

export default BulkSlice.reducer;